{
  "name": "penca-hockey-2026",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:server": "node server/index.js",
    "dev:client": "vite client --port 3000",
    "build": "vite build client --outDir ../dist",
    "start": "npm run build && node server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.1.4"
  },
  "engines": {
    "node": ">=18"
  }
}
